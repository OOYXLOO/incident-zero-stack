# Demo Script

1. Start on the dashboard and point out `Local review build`, `No secrets stored`, and `AWS proof required`.
2. Show the case panel: critical alert, risk score, impacted system, and signals.
3. Show response tasks and explain owner mapping.
4. Show database records and explain the DynamoDB single-table shape.
5. Show the audit timeline and executive handoff record.
6. Open `/api/case` to show the same data as JSON.
7. Open `/api/schema` to show the AWS Database design.
8. End on the cloud proof gate: live Vercel/v0 deployment and AWS Database screenshot are required before final H0 submission.
